# KG Platform Backend Requirements
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0

# Auth
PyJWT>=2.8
passlib[bcrypt]>=1.7
bcrypt>=4.0

# Encryption
cryptography>=42.0

# LLM / AI
openai>=1.0
httpx>=0.27
anthropic>=0.30

# Parsers
pdfplumber>=0.11
PyMuPDF>=1.24
python-docx>=1.1
openpyxl>=3.1
rdflib>=7.0

# NER / Extraction (heavy — 默认开启；如仅需部分引擎可注释对应行)
spacy>=3.7
gliner>=0.1
paddlenlp>=3.0
# 使用 GPU（如 T4）时把下面这行换成 paddlepaddle-gpu>=3.0；纯 CPU 环境保持 paddlepaddle
paddlepaddle>=3.0
paddleocr>=2.7
modelscope>=1.14
mineru[all]

# Misc
aiofiles>=24.0
