# KG Platform Backend Requirements
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0

# Auth
PyJWT>=2.8
passlib[bcrypt]>=1.7
bcrypt>=4.0

# Encryption
cryptography>=42.0

# LLM / AI
openai>=1.0
httpx>=0.27
anthropic>=0.30

# Parsers
pdfplumber>=0.11
PyMuPDF>=1.24
python-docx>=1.1
openpyxl>=3.1
rdflib>=7.0

# NER / Extraction (heavy — 默认开启；如仅需部分引擎可注释对应行)
spacy>=3.7
gliner>=0.1
# 显式 pin scipy 到带 cp311 manylinux 预编译 wheel 的版本，
# 否则 pip 会回退到需源码编译的旧版（slim 镜像缺编译工具会失败）。
# flair==0.13.1 约束 scipy<=1.12，1.11.4 在此范围内且有 wheel。
scipy==1.11.4
# ---- Paddle 全家桶 (GPU 版, T4) ----
# 版本号对齐已验证可用的组合:
#   paddle2onnx 2.1.0 / paddlefsl 1.1.0 / paddlenlp 2.6.1 / paddleocr 3.6.0 / paddlepaddle-gpu 2.6.2
# 注意: PaddlePaddle 稳定版最高仅 2.6.x（3.0 仅有 paddlenlp 的 beta），故统一用 >=2.6.0
paddle2onnx>=2.1.0
paddlefsl>=1.1.0
paddlenlp>=2.6.0
paddlepaddle-gpu>=2.6.0
paddleocr>=3.5.0
modelscope>=1.14
# ---- PyTorch (CUDA 构建, 供 gliner / mineru / DeepKE / spaCy-transformer 使用 GPU) ----
# 必须显式 pin 到 +cu121 本地版本: 否则 pip 会优先选 PyPI 上的 CPU 版 torch, GPU 失效
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
mineru[all]

# Misc
aiofiles>=24.0
